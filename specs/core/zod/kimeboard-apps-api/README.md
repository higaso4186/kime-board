This zip contains `apps/api` skeleton (Next.js Route Handlers) designed to work with `packages/shared`.

Place it into your monorepo:
- apps/api
- packages/shared (already created previously)

Then install:
- pnpm install

Build:
- pnpm -C apps/api build

Deploy to Cloud Run with Next standalone output.
