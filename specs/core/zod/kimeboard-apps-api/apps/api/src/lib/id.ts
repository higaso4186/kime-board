import { newId } from "@kimeboard/shared";

export const newProjectId = () => `prj_${newId()}`;
export const newMeetingId = () => `mtg_${newId()}`;
export const newDecisionId = () => `dcs_${newId()}`;
export const newActionId = () => `act_${newId()}`;
export const newThreadId = () => `thr_${newId()}`;
export const newMessageId = () => `msg_${newId()}`;
export const newNotificationId = () => `ntf_${newId()}`;
export const newRunId = () => `run_${newId()}`;
