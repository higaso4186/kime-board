rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder: tighten with project membership checks before production.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
